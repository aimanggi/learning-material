// Single Number
// Given a non-empty array of integers, every element appears twice except for one. Find that single one.

const array1 = [2, 3, 2, 1, 3];
const array2 = [2,2,1]
const array3 = [4,1,2,1,2]

const singleNumber = arr => {
    // write code here
}

console.log(singleNumber(array1)) // 1
console.log(singleNumber(array2)) // 1
console.log(singleNumber(array3)) // 4
